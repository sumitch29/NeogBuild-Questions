//Write a program to find the maximum and minimum out of two given numbers. The numbers are num1=129 and num2=251.

const maximin = (num1, num2) => {
  if (num1 > num2) {
    console.log(`${num1} is greater than ${num2}`);
  } else {
   c;
  }
};
maximin(129, 251);

                                        //or
const maximin = (num1, num2) =>
  num1 > num2
    ? console.log(`${num1} is greater than ${num2}`)
    : console.log(`${num1} is greater than ${num2}`);
maximin(129, 251);
